# Qt Framework (qt_compat.py tenta PySide6 primeiro, fallback PyQt5)
PyQt5>=5.15; sys_platform == "win32"
PySide6>=6.5; sys_platform == "darwin"

# Dependências do projeto
pandas>=2.0
openpyxl>=3.1
requests>=2.31
pyinstaller>=6.0
